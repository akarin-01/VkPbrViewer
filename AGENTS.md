# VkPbrViewer — Agent 会话指引

Vulkan PBR 渲染器（C++17 / MSVC / Win32）。本文件每次会话自动加载，承担**文档导航**职责。

## 工作模式（默认顾问，改码需授权）

- 默认只输出分析、建议、方案对比、示例代码与审查结论，**不落盘修改代码**。
- 只有用户明确要求改动（"实现 / 修改 / 重构 / 修复 X"）才修改代码；"建议怎么改"类请求默认不动代码。
- 例外：`session_log.md` 收尾追加与进度同步属既定纪律，随被授权的任务一并执行；聊天中未明确要求的文档改动同样不做。
- 任何建议的方案/决策若与 `docs/architecture.md` 或现有代码设计冲突，**必须明确指出冲突点与影响**，不得静默按新方案执行；用户已明确选择该方案后再照做，并在结果中说明冲突。

## 文档地图

| 文件 | 角色 | 何时读 |
| --- | --- | --- |
| `docs/implementation/00_overview.md` | 实现步骤索引 + 当前进度 | 每次会话必读 |
| `docs/session_log.md` | 跨会话手记（最后一条 = 上次进展与遗留） | 每次会话必读最后一条 |
| `docs/architecture.md` | 跨层架构约定（分层/命名/所有权/数据流/错误处理） | 动代码前、涉及架构时 |
| `docs/build_notes.md` | 构建/运行命令与环境坑 | 构建、运行、配置时 |
| `docs/implementation/01~08_*.md` | 各步骤实现细节 | 涉及对应功能时 |
| `docs/requirements.md`、`docs/project_idea.md` | 需求基线（**仅作参考，以代码为准**） | 需求相关任务时 |

读取顺序：`00_overview.md` → `session_log.md` 最后一条 → 按任务需要读其余文件。

## 变更纪律

- 动代码前后核对 `docs/architecture.md`（分层 / 命名 / 所有权 / 数据流约定）。
- 构建与运行命令见 `docs/build_notes.md`（注意：VS 生成器需 `--config Debug`；exe 需在仓库根运行）。
- 实现完成后同步更新对应实现步骤文档；文档永远反映落地后的真实结构。
- 新决策、环境坑、会话进展 → 追加到 `docs/session_log.md`（格式见该文件首条）。
- 发现 `requirements.md` / `project_idea.md` 与代码不一致时，**以实际代码为准**，并顺手更新这两个文件消除偏差。
- 保持文档精简索引式；不要新增长篇说明文档。